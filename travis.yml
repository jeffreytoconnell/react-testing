language: node_js
node_js: node
cache:
  directories:
    - node_modules
script:
  - npm test
env:
  - CI=true

before_deploy:
  - npm install netlify-cli -g
  - npm run build

deploy:
  provider: script
  script: netlify deploy -s $editor-lesson-55041 -t $faf579f5-0226-4f6f-a119-af31093a4b85 -p ./build
  skip_cleanup: true
  on:
    branch: master
